machine:
  node:
    version: v0.12.4

dependencies:
  override:
    - npm install

test:
  override:
    - npm run lint
    - npm run test

deployment:
  coverage:
    branch: master
    commands:
      - npm run coveralls