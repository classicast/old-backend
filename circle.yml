machine:
  node:
    version: v0.12.4

dependencies:
  override:
    - npm install

test:
  override:
    - npm run lint
    - npm run test

deployment:
  production:
    branch: master
    commands:
      - npm run coveralls
      - git push git@heroku.com:api-classicaldb.git $CIRCLE_SHA1:master